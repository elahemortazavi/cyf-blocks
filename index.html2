<html>
<p>my code block html</p>
<span id="text"></span>
<button id="button">select facilitator</button>

<script>
var list;

function randomInt(n) {
  // Return a random number from in [0, n[
  return Math.floor(Math.random()*n);
}

function randomMember(arr) {
  // Return a random member of the array
  return arr[randomInt(arr.length)]
}


list = ['hard', 'soon', 'easy', 'hope', 'future', 'do'];


document.getElementById('button').addEventListener('click', (event) => {
  let element_text = document.getElementById('text');
  let new_li = document.createElement('li');
  new_li.innerText = randomMember(list);

  element_text.appendChild(new_li);

});

</script>
</html>
